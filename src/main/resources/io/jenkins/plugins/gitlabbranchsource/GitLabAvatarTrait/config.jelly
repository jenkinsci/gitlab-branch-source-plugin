<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry title="${%Disable GitLab project avatar}" field="disableProjectAvatar">
    <f:checkbox default="checked"/><!-- Change default to unchecked when more options are added -->
  </f:entry>
</j:jelly>
